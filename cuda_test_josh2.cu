#include <iostream>
#include <string>

/*
__global__ void getMeSize(int *result) {
     result[0]=sizeof(void*);
}

__global__ void  t(int *d){
  atomicAdd(d, 1);
}
*/ 

int main() { 

/*	
   #ifdef __CUDACC__
      std::cout << "__CUDACC__" << "\n";
   #else
      std::cout << "No __CUDACC__" << "\n";
   #endif 

   #ifdef __NVCC__
      std::cout << "__NVCC__" << "\n";
   #else
      std::cout << "No __NVCC__" << "\n";
   #endif 

   #ifdef __CUDA_ARCH__
      std::cout << "__CUDA_ARCH__" << "\n";
   #else
      std::cout << "No __CUDA_ARCH__" << "\n";
   #endif
*/
   return 0;   
}


